# vim: et ft=ruby ts=4 sw=4

include ./config;


include ../include/dust/std;
include ../include/dust/lib.term;
include ../include/dust/lib.ide;

include ./lib.install;
def archive = !git archive HEAD --format tar;
include ./lib.docker;

def lib-init = {
    ;
}

def ide::compile_all = {
    ide::compile ci/lib;
    ide::compile var/web-app/lib;

    !lsd --tree $_.lib.ide/target_path;
}
